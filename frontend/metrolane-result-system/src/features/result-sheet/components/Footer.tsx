import { formatDisplayDate } from "@/features/result-sheet/utils/resultHelpers"

interface FooterProps {
  generatedAt: Date
  pageNumber?: number
  totalPages?: number
}

export function Footer({
  generatedAt,
  pageNumber = 1,
  totalPages = 1,
}: FooterProps) {
  return (
    <footer className="border-t border-gray-200 pt-4 text-center text-xs text-gray-500">
      <p className="font-medium text-gray-700">
        METROLANE College of Health Sciences and Technology
      </p>
      <p className="mt-1">
        This result was electronically generated by the Lecturer Result Management
        System.
      </p>
      <div className="mt-2 flex items-center justify-between text-[11px]">
        <span>Generated Date: {formatDisplayDate(generatedAt)}</span>
        <span>
          Page {pageNumber} of {totalPages}
        </span>
      </div>
    </footer>
  )
}
